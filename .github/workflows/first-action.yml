name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Greetings! Hello to the first workflow"
      - name: Farewell
        run: echo "bye-bye from first workflow"
